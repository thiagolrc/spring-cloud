---
spring:
  application:
    name: app-sentence
  cloud:
    config:
      uri: http://localhost:8001
      failFast: true

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream